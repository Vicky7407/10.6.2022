
const initVal ={
    isLoading:false,
    user:null,
    error:''

}

export const reducerAuth =(state=initVal,action) =>{
     switch (action.type) {
        default:
            return state;
     }
}